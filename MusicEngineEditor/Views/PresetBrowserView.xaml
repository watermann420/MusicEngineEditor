<UserControl x:Class="MusicEngineEditor.Views.PresetBrowserView"
             x:Name="PresetBrowserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MusicEngineEditor.Views"
             xmlns:controls="clr-namespace:MusicEngineEditor.Controls"
             xmlns:viewmodels="clr-namespace:MusicEngineEditor.ViewModels"
             xmlns:models="clr-namespace:MusicEngineEditor.Models"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="400"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Search TextBox Style -->
        <Style x:Key="PresetSearchTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="&#x1F50D;"
                                           FontSize="12"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,4,0"/>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Grid.Column="1"
                                              Margin="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>
                                <TextBlock x:Name="placeholder"
                                           Grid.Column="1"
                                           Text="Search presets..."
                                           FontSize="12"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                                <Button x:Name="clearButton"
                                        Grid.Column="2"
                                        Content="&#x2715;"
                                        FontSize="10"
                                        Background="Transparent"
                                        BorderThickness="0"
                                        Foreground="{DynamicResource SecondaryForegroundBrush}"
                                        Padding="8,0"
                                        Cursor="Hand"
                                        Visibility="Collapsed"
                                        Click="ClearSearch_Click"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="Text" Value="">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                            </Trigger>
                            <DataTrigger Binding="{Binding Text.Length, RelativeSource={RelativeSource Self}}" Value="0">
                                <Setter TargetName="placeholder" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="clearButton" Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="clearButton" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Filter Button Style -->
        <Style x:Key="FilterButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SubtleBorderBrush}"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3C3F41"/>
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Category Button Style -->
        <Style x:Key="CategoryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3"
                                Margin="0,0,4,0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3C3F41"/>
                                <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="Tag" Value="Active">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tag Button Style -->
        <Style x:Key="TagButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="{DynamicResource SubtleBorderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,3"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#4C4F51"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Tree View Item Style -->
        <Style x:Key="BankTreeItemStyle" TargetType="TreeViewItem">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="IsExpanded" Value="True"/>
        </Style>

        <!-- View Toggle Button Style -->
        <Style x:Key="ViewToggleButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#3C3F41"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="&#x1F3B9;"
                               FontSize="16"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <TextBlock Text="Preset Browser"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource BrightForegroundBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <!-- View Toggle -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <ToggleButton Style="{StaticResource ViewToggleButtonStyle}"
                                  IsChecked="{Binding IsGridView}"
                                  ToolTip="Grid View">
                        <TextBlock Text="&#x25A6;" FontSize="14"/>
                    </ToggleButton>
                    <ToggleButton Style="{StaticResource ViewToggleButtonStyle}"
                                  IsChecked="{Binding IsGridView, Converter={local:InverseBoolConverter}}"
                                  ToolTip="List View"
                                  Click="ListViewToggle_Click">
                        <TextBlock Text="&#x2630;" FontSize="14"/>
                    </ToggleButton>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Search Box -->
        <Border Grid.Row="1" Padding="12,8">
            <TextBox x:Name="SearchTextBox"
                     Style="{StaticResource PresetSearchTextBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"/>
        </Border>

        <!-- Filter Buttons -->
        <Border Grid.Row="2" Padding="12,4,12,8">
            <WrapPanel>
                <ToggleButton Content="Synths"
                              Style="{StaticResource FilterButtonStyle}"
                              IsChecked="{Binding SelectedTargetType, Converter={local:TargetTypeConverter}, ConverterParameter=Synth}"
                              Click="SynthFilter_Click"
                              Margin="0,0,4,4"/>
                <ToggleButton Content="Effects"
                              Style="{StaticResource FilterButtonStyle}"
                              IsChecked="{Binding SelectedTargetType, Converter={local:TargetTypeConverter}, ConverterParameter=Effect}"
                              Click="EffectFilter_Click"
                              Margin="0,0,4,4"/>
                <ToggleButton Content="&#x2665; Favorites"
                              Style="{StaticResource FilterButtonStyle}"
                              IsChecked="{Binding ShowOnlyFavorites}"
                              Margin="0,0,4,4"/>
            </WrapPanel>
        </Border>

        <!-- Category Tabs -->
        <Border Grid.Row="3" Padding="12,0,12,8">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Disabled">
                <ItemsControl ItemsSource="{Binding Categories}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Button Content="{Binding}"
                                    Style="{StaticResource CategoryButtonStyle}"
                                    Tag="{Binding DataContext.SelectedCategory, ElementName=PresetBrowserControl, Converter={local:CategoryActiveConverter}, ConverterParameter={Binding}}"
                                    Click="Category_Click"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <!-- Main Content Area -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" MinWidth="0"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Hierarchical Tree View (Banks > Categories) -->
            <Border Grid.Column="0"
                    Width="180"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,1,0"
                    Visibility="{Binding IsGridView, Converter={local:BoolToVisibilityConverter}, ConverterParameter=Inverse}">
                <TreeView x:Name="BankTreeView"
                          ItemsSource="{Binding BankNodes}"
                          Background="Transparent"
                          BorderThickness="0"
                          Padding="4"
                          SelectedItemChanged="BankTreeView_SelectedItemChanged">
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type models:PresetBankNode}"
                                                  ItemsSource="{Binding Categories}">
                            <TextBlock Text="{Binding DisplayName}"
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource ForegroundBrush}"/>
                            <HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate DataType="{x:Type models:PresetCategoryNode}">
                                    <TextBlock Text="{Binding DisplayName}"
                                               Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                                </DataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem" BasedOn="{StaticResource BankTreeItemStyle}"/>
                    </TreeView.ItemContainerStyle>
                </TreeView>
            </Border>

            <!-- Preset Grid/List -->
            <Grid Grid.Column="1">
                <!-- Grid View -->
                <ItemsControl x:Name="PresetGridView"
                              ItemsSource="{Binding FilteredPresets}"
                              Visibility="{Binding IsGridView, Converter={local:BoolToVisibilityConverter}}"
                              Padding="8">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <controls:PresetCard Preset="{Binding}"
                                                 Width="160"
                                                 Margin="4"
                                                 MouseDoubleClick="PresetCard_MouseDoubleClick"
                                                 PreviewMouseLeftButtonDown="PresetCard_PreviewMouseLeftButtonDown">
                                <controls:PresetCard.ContextMenu>
                                    <ContextMenu>
                                        <MenuItem Header="Load Preset" Click="ContextMenu_LoadPreset_Click"/>
                                        <Separator/>
                                        <MenuItem Header="Toggle Favorite" Click="ContextMenu_ToggleFavorite_Click"/>
                                        <Separator/>
                                        <MenuItem Header="Rename..." Click="ContextMenu_Rename_Click"/>
                                        <MenuItem Header="Delete" Click="ContextMenu_Delete_Click"/>
                                    </ContextMenu>
                                </controls:PresetCard.ContextMenu>
                            </controls:PresetCard>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- List View -->
                <ListView x:Name="PresetListView"
                          ItemsSource="{Binding FilteredPresets}"
                          SelectedItem="{Binding SelectedPreset}"
                          Visibility="{Binding IsGridView, Converter={local:BoolToVisibilityConverter}, ConverterParameter=Inverse}"
                          Background="Transparent"
                          BorderThickness="0"
                          Padding="4"
                          MouseDoubleClick="PresetListView_MouseDoubleClick">
                    <ListView.ContextMenu>
                        <ContextMenu>
                            <MenuItem Header="Load Preset" Click="ContextMenu_LoadPreset_Click"/>
                            <Separator/>
                            <MenuItem Header="Toggle Favorite" Click="ContextMenu_ToggleFavorite_Click"/>
                            <Separator/>
                            <MenuItem Header="Rename..." Click="ContextMenu_Rename_Click"/>
                            <MenuItem Header="Delete" Click="ContextMenu_Delete_Click"/>
                        </ContextMenu>
                    </ListView.ContextMenu>
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" Width="140" DisplayMemberBinding="{Binding Name}"/>
                            <GridViewColumn Header="Category" Width="80" DisplayMemberBinding="{Binding Category}"/>
                            <GridViewColumn Header="Author" Width="80" DisplayMemberBinding="{Binding Author}"/>
                            <GridViewColumn Header="Type" Width="60" DisplayMemberBinding="{Binding TargetTypeDisplay}"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>
        </Grid>

        <!-- Tag Cloud -->
        <Expander Grid.Row="5"
                  Header="Tags"
                  IsExpanded="False"
                  Foreground="{DynamicResource ForegroundBrush}"
                  BorderBrush="{DynamicResource BorderBrush}"
                  BorderThickness="0,1,0,0"
                  Padding="8">
            <ItemsControl ItemsSource="{Binding Tags}" Margin="4">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ToggleButton Style="{StaticResource TagButtonStyle}"
                                      IsChecked="{Binding IsSelected}"
                                      FontSize="{Binding FontSize}"
                                      Click="Tag_Click">
                            <TextBlock Text="{Binding Name}"/>
                        </ToggleButton>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Expander>

        <!-- Status Bar -->
        <Border Grid.Row="6"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusMessage}"
                           FontSize="11"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           VerticalAlignment="Center"/>

                <Button Grid.Column="1"
                        Content="&#x25B6; Preview"
                        Padding="10,4"
                        FontSize="11"
                        Margin="0,0,8,0"
                        Command="{Binding PreviewCommand}"
                        IsEnabled="{Binding SelectedPreset, Converter={local:NotNullToBoolConverter}}"
                        Style="{DynamicResource SecondaryButtonStyle}"/>

                <Button Grid.Column="2"
                        Content="Load Preset"
                        Style="{DynamicResource PrimaryButtonStyle}"
                        Padding="12,4"
                        FontSize="11"
                        Command="{Binding LoadPresetCommand}"
                        IsEnabled="{Binding SelectedPreset, Converter={local:NotNullToBoolConverter}}"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
