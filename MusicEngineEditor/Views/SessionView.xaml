<UserControl x:Class="MusicEngineEditor.Views.SessionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:MusicEngineEditor.Controls"
             xmlns:vm="clr-namespace:MusicEngineEditor.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance Type=vm:SessionViewModel}"
             Background="#0D0D0D">

    <UserControl.InputBindings>
        <KeyBinding Key="Space" Command="{Binding StartCommand}"/>
        <KeyBinding Key="Space" Modifiers="Shift" Command="{Binding StopCommand}"/>
        <KeyBinding Key="Escape" Command="{Binding StopAllCommand}"/>
        <KeyBinding Key="Home" Command="{Binding ResetCommand}"/>
    </UserControl.InputBindings>

    <UserControl.Resources>
        <!-- Colors -->
        <Color x:Key="BackgroundColor">#0D0D0D</Color>
        <Color x:Key="PanelColor">#181818</Color>
        <Color x:Key="BorderColor">#2A2A2A</Color>
        <Color x:Key="AccentColor">#00D9FF</Color>
        <Color x:Key="SuccessColor">#00FF88</Color>
        <Color x:Key="ErrorColor">#FF4757</Color>
        <Color x:Key="WarningColor">#FFB800</Color>
        <Color x:Key="TextPrimaryColor">#E0E0E0</Color>
        <Color x:Key="TextSecondaryColor">#808080</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="BackgroundBrush" Color="{StaticResource BackgroundColor}"/>
        <SolidColorBrush x:Key="PanelBrush" Color="{StaticResource PanelColor}"/>
        <SolidColorBrush x:Key="BorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="{StaticResource SuccessColor}"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="{StaticResource ErrorColor}"/>
        <SolidColorBrush x:Key="WarningBrush" Color="{StaticResource WarningColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>

        <!-- Toolbar Button Style -->
        <Style x:Key="ToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#252525"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                <Setter TargetName="border" Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Play Button Style -->
        <Style x:Key="PlayButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}">
            <Setter Property="Background" Value="{StaticResource SuccessBrush}"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Path x:Name="PlayIcon"
                                      Data="M 0,0 L 10,6 L 0,12 Z"
                                      Fill="Black"
                                      VerticalAlignment="Center"
                                      Margin="0,0,6,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#20FF99"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#00CC66"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Stop Button Style -->
        <Style x:Key="StopButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolbarButtonStyle}">
            <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <Rectangle Width="10" Height="10"
                                           Fill="White"
                                           VerticalAlignment="Center"
                                           Margin="0,0,6,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#FF6B7A"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background">
                                    <Setter.Value>
                                        <SolidColorBrush Color="#CC3944"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ComboBox Style -->
        <Style x:Key="DarkComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="MinWidth" Value="100"/>
        </Style>

        <!-- BoolToVis Converter -->
        <BooleanToVisibilityConverter x:Key="BoolToVisConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Top Toolbar -->
        <Border Grid.Row="0"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="8,6">
            <DockPanel>
                <!-- Left Section: Transport Controls -->
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <!-- Play Button -->
                    <Button Style="{StaticResource PlayButtonStyle}"
                            Content="Play"
                            Command="{Binding StartCommand}"
                            ToolTip="Start Session (Space)"/>

                    <!-- Stop Button -->
                    <Button Style="{StaticResource StopButtonStyle}"
                            Content="Stop"
                            Command="{Binding StopCommand}"
                            ToolTip="Stop Session (Shift+Space)"/>

                    <!-- Stop All Button -->
                    <Button Style="{StaticResource ToolbarButtonStyle}"
                            Content="Stop All"
                            Command="{Binding StopAllCommand}"
                            ToolTip="Stop All Clips (Escape)"/>

                    <!-- Reset Button -->
                    <Button Style="{StaticResource ToolbarButtonStyle}"
                            Content="Reset"
                            Command="{Binding ResetCommand}"
                            ToolTip="Reset to Beat 0 (Home)"/>

                    <Rectangle Width="1"
                               Fill="{StaticResource BorderBrush}"
                               Margin="12,4"/>

                    <!-- Add Scene Button -->
                    <Button Style="{StaticResource ToolbarButtonStyle}"
                            Content="+ Scene"
                            Command="{Binding AddSceneCommand}"
                            ToolTip="Add New Scene"/>
                </StackPanel>

                <!-- Center Section: Quantize and BPM -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <!-- Quantize Selector -->
                    <TextBlock Text="Quantize:"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0"
                               FontSize="11"/>
                    <ComboBox Style="{StaticResource DarkComboBoxStyle}"
                              ItemsSource="{Binding QuantizeOptions}"
                              SelectedItem="{Binding SelectedQuantize}"
                              MinWidth="120"
                              ToolTip="Launch Quantization">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="{Binding}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding}" Value="None">
                                                    <Setter Property="Text" Value="None"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding}" Value="OneBar">
                                                    <Setter Property="Text" Value="1 Bar"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding}" Value="HalfBar">
                                                    <Setter Property="Text" Value="1/2 Bar"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding}" Value="QuarterNote">
                                                    <Setter Property="Text" Value="1/4 Note"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding}" Value="EighthNote">
                                                    <Setter Property="Text" Value="1/8 Note"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding}" Value="SixteenthNote">
                                                    <Setter Property="Text" Value="1/16 Note"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <Rectangle Width="1"
                               Fill="{StaticResource BorderBrush}"
                               Margin="16,4"/>

                    <!-- BPM Display -->
                    <TextBlock Text="BPM:"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0"
                               FontSize="11"/>
                    <TextBox Text="{Binding Bpm, UpdateSourceTrigger=PropertyChanged, StringFormat=F1}"
                             Background="{StaticResource PanelBrush}"
                             Foreground="{StaticResource AccentBrush}"
                             BorderBrush="{StaticResource BorderBrush}"
                             BorderThickness="1"
                             Padding="8,4"
                             MinWidth="60"
                             FontSize="14"
                             FontWeight="SemiBold"
                             TextAlignment="Center"
                             ToolTip="Tempo (BPM)"/>

                    <Rectangle Width="1"
                               Fill="{StaticResource BorderBrush}"
                               Margin="16,4"/>

                    <!-- Beat Position Display -->
                    <TextBlock Text="Beat:"
                               Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,6,0"
                               FontSize="11"/>
                    <Border Background="#101010"
                            BorderBrush="{StaticResource BorderBrush}"
                            BorderThickness="1"
                            CornerRadius="4"
                            Padding="8,4">
                        <TextBlock Text="{Binding CurrentBeat, StringFormat=F2}"
                                   Foreground="{StaticResource SuccessBrush}"
                                   FontSize="14"
                                   FontWeight="SemiBold"
                                   FontFamily="Consolas"
                                   MinWidth="50"
                                   TextAlignment="Center"/>
                    </Border>
                </StackPanel>

                <!-- Right Section: Status -->
                <StackPanel Orientation="Horizontal"
                            DockPanel.Dock="Right"
                            HorizontalAlignment="Right">
                    <!-- Running Indicator -->
                    <Border CornerRadius="4"
                            Padding="8,4"
                            Margin="4,0">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{StaticResource PanelBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                        <Setter Property="Background" Value="#203020"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8"
                                     VerticalAlignment="Center"
                                     Margin="0,0,6,0">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="{StaticResource TextSecondaryBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                <Setter Property="Fill" Value="{StaticResource SuccessBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock VerticalAlignment="Center" FontSize="11">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="Stopped"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                                <Setter Property="Text" Value="Running"/>
                                                <Setter Property="Foreground" Value="{StaticResource SuccessBrush}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </Border>

                    <!-- Grid Size Display -->
                    <TextBlock Foreground="{StaticResource TextSecondaryBrush}"
                               VerticalAlignment="Center"
                               FontSize="11"
                               Margin="8,0">
                        <Run Text="{Binding TrackCount, Mode=OneWay}"/>
                        <Run Text="x"/>
                        <Run Text="{Binding SceneCount, Mode=OneWay}"/>
                    </TextBlock>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Main Content: Clip Launcher Grid -->
        <controls:ClipLauncherGrid Grid.Row="1"
                                    ViewModel="{Binding}"/>

        <!-- Bottom Status Bar -->
        <Border Grid.Row="2"
                Background="{StaticResource PanelBrush}"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="8,4">
            <DockPanel>
                <!-- Left: Status Message -->
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="11"
                           VerticalAlignment="Center"
                           DockPanel.Dock="Left"/>

                <!-- Right: Help Text -->
                <TextBlock Foreground="{StaticResource TextSecondaryBrush}"
                           FontSize="10"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           DockPanel.Dock="Right">
                    <Run Text="Click: Launch"/>
                    <Run Text=" | " Foreground="#404040"/>
                    <Run Text="Double-Click: Edit"/>
                    <Run Text=" | " Foreground="#404040"/>
                    <Run Text="Right-Click: Options"/>
                </TextBlock>

                <Border/>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
