<Window x:Class="MusicEngineEditor.Views.Dialogs.GrooveTemplateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:MusicEngineEditor.Converters"
        mc:Ignorable="d"
        Title="Groove Templates"
        Width="600" Height="500"
        MinWidth="500" MinHeight="400"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">

    <Window.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <!-- Template List Item Style -->
        <Style x:Key="TemplateListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3C3F41"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="16"/>
            <ColumnDefinition Width="200"/>
        </Grid.ColumnDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Grid.ColumnSpan="3"
                   Text="Groove Templates"
                   FontSize="18"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16"/>

        <!-- Template Browser (Left Panel) -->
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Category Filter -->
            <Grid Grid.Row="0" Margin="0,0,0,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Category:"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"/>

                <ComboBox Grid.Column="1"
                          x:Name="CategoryComboBox"
                          SelectionChanged="CategoryComboBox_SelectionChanged">
                    <ComboBoxItem Content="All" IsSelected="True"/>
                    <ComboBoxItem Content="MPC Swing"/>
                    <ComboBoxItem Content="Shuffle"/>
                    <ComboBoxItem Content="Hip-Hop"/>
                    <ComboBoxItem Content="Funk"/>
                    <ComboBoxItem Content="Jazz"/>
                    <ComboBoxItem Content="Electronic"/>
                    <ComboBoxItem Content="Other"/>
                </ComboBox>
            </Grid>

            <!-- Template List -->
            <ListBox Grid.Row="1"
                     x:Name="TemplateListBox"
                     Background="Transparent"
                     BorderBrush="{DynamicResource BorderBrush}"
                     BorderThickness="1"
                     ItemContainerStyle="{StaticResource TemplateListItemStyle}"
                     SelectionChanged="TemplateListBox_SelectionChanged">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="{Binding Name}"
                                           FontWeight="SemiBold"
                                           FontSize="12"/>
                                <TextBlock Text="{Binding Category}"
                                           FontSize="10"
                                           Opacity="0.7"/>
                            </StackPanel>

                            <Border Grid.Column="1"
                                    Visibility="{Binding IsBuiltIn, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}"
                                    Background="{DynamicResource AccentBrush}"
                                    CornerRadius="8"
                                    Padding="6,2">
                                <TextBlock Text="Built-in"
                                           FontSize="9"
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <!-- Preview Panel (Right) -->
        <Border Grid.Row="1" Grid.Column="2"
                Background="{DynamicResource BackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Preview"
                           FontWeight="SemiBold"
                           Margin="0,0,0,12"/>

                <!-- Timing Deviation Graph -->
                <Border Grid.Row="1"
                        Background="#1E1F22"
                        CornerRadius="4"
                        Margin="0,0,0,12">
                    <Canvas x:Name="DeviationCanvas"
                            Height="100"
                            ClipToBounds="True"/>
                </Border>

                <!-- Template Info -->
                <TextBlock Grid.Row="2"
                           x:Name="TemplateInfoText"
                           Text="Select a template"
                           FontSize="11"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <!-- Options Section -->
        <Grid Grid.Row="2" Grid.ColumnSpan="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Amount Slider -->
            <Grid Grid.Row="0" Grid.Column="0" Margin="0,0,16,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Amount:"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0"/>

                <Slider Grid.Column="1"
                        x:Name="AmountSlider"
                        Minimum="0"
                        Maximum="100"
                        Value="100"
                        VerticalAlignment="Center"/>

                <TextBlock Grid.Column="2"
                           Text="{Binding ElementName=AmountSlider, Path=Value, StringFormat='{}{0:F0}%'}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"/>
            </Grid>

            <!-- Options Checkboxes -->
            <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal">
                <CheckBox x:Name="ApplyTimingCheckBox"
                          Content="Timing"
                          IsChecked="True"
                          Margin="0,0,16,0"/>

                <CheckBox x:Name="ApplyVelocityCheckBox"
                          Content="Velocity"
                          IsChecked="True"
                          Margin="0,0,16,0"/>

                <CheckBox x:Name="QuantizeFirstCheckBox"
                          Content="Quantize First"/>
            </StackPanel>

            <!-- Save Current -->
            <Button Grid.Row="1" Grid.Column="0"
                    Content="Save Current as Template..."
                    Click="SaveAsTemplateButton_Click"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    HorizontalAlignment="Left"
                    Padding="16,8"/>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Grid.ColumnSpan="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Preview"
                    Click="PreviewButton_Click"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    Padding="20,8"
                    Margin="0,0,8,0"/>

            <Button Content="Apply"
                    Click="ApplyButton_Click"
                    Style="{DynamicResource PrimaryButtonStyle}"
                    Padding="20,8"
                    Margin="0,0,8,0"
                    IsDefault="True"/>

            <Button Content="Cancel"
                    Click="CancelButton_Click"
                    Style="{DynamicResource SecondaryButtonStyle}"
                    Padding="20,8"
                    IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
