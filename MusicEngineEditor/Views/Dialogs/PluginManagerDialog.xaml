<Window x:Class="MusicEngineEditor.Views.Dialogs.PluginManagerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Plugin Manager"
        Width="1000" Height="700"
        MinWidth="800" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource BackgroundBrush}"
        Foreground="{DynamicResource ForegroundBrush}">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Format badge colors -->
        <SolidColorBrush x:Key="Vst2BadgeBrush" Color="#9C7CE8"/>
        <SolidColorBrush x:Key="Vst3BadgeBrush" Color="#6AAB73"/>
        <SolidColorBrush x:Key="FavoriteBrush" Color="#FFD700"/>
        <SolidColorBrush x:Key="DisabledBrush" Color="#666666"/>

        <!-- Category list item style -->
        <Style x:Key="CategoryListItemStyle" TargetType="ListBoxItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3C3F41"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Plugin row style -->
        <Style x:Key="PluginRowStyle" TargetType="DataGridRow">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3C3F41"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <DataTrigger Binding="{Binding IsEnabled}" Value="False">
                    <Setter Property="Opacity" Value="0.5"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Star button style -->
        <Style x:Key="StarButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border Background="{TemplateBinding Background}" Padding="4">
                            <TextBlock x:Name="Star"
                                       Text="*"
                                       FontSize="16"
                                       FontWeight="Bold"
                                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Star" Property="Foreground" Value="{StaticResource FavoriteBrush}"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Star" Property="Foreground" Value="{StaticResource FavoriteBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Search and Actions -->
        <Border Grid.Row="0"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="16,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Search Box -->
                <Border Grid.Column="0"
                        Background="#252628"
                        BorderBrush="{DynamicResource BorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="10,8"
                        MaxWidth="400"
                        HorizontalAlignment="Left">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Column="0"
                                   Text="Search"
                                   FontSize="12"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,10,0"/>

                        <TextBox x:Name="SearchBox"
                                 Grid.Column="1"
                                 Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Foreground="{DynamicResource ForegroundBrush}"
                                 CaretBrush="{DynamicResource ForegroundBrush}"
                                 FontSize="13"
                                 VerticalAlignment="Center"
                                 MinWidth="200"/>

                        <Button Grid.Column="2"
                                Content="X"
                                Click="ClearSearch_Click"
                                Background="Transparent"
                                BorderThickness="0"
                                Foreground="{DynamicResource SecondaryForegroundBrush}"
                                FontSize="12"
                                Padding="6,2"
                                Cursor="Hand"
                                VerticalAlignment="Center"
                                Visibility="{Binding HasSearchText, Converter={StaticResource BoolToVisibilityConverter}}"/>
                    </Grid>
                </Border>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Rescan Plugins"
                            Click="RescanPlugins_Click"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Padding="12,8"
                            Margin="0,0,8,0"/>
                    <Button Content="Add Folder..."
                            Click="AddFolder_Click"
                            Style="{DynamicResource SecondaryButtonStyle}"
                            Padding="12,8"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" MinWidth="150"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="280" MinWidth="200"/>
            </Grid.ColumnDefinitions>

            <!-- Categories Panel -->
            <Border Grid.Column="0"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,1,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="12,10">
                        <TextBlock Text="Categories"
                                   FontWeight="SemiBold"
                                   FontSize="12"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                    </Border>

                    <ListBox Grid.Row="1"
                             x:Name="CategoryList"
                             ItemsSource="{Binding Categories}"
                             SelectedItem="{Binding SelectedCategory}"
                             ItemContainerStyle="{StaticResource CategoryListItemStyle}"
                             Background="Transparent"
                             BorderThickness="0"
                             Padding="4"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0"
                                               Text="{Binding Name}"
                                               FontSize="12"
                                               VerticalAlignment="Center"/>

                                    <Border Grid.Column="1"
                                            Background="{DynamicResource SecondaryForegroundBrush}"
                                            CornerRadius="8"
                                            Padding="6,2"
                                            Margin="6,0,0,0">
                                        <TextBlock Text="{Binding Count}"
                                                   FontSize="10"
                                                   Foreground="{DynamicResource BackgroundBrush}"
                                                   FontWeight="SemiBold"
                                                   HorizontalAlignment="Center"/>
                                    </Border>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <GridSplitter Grid.Column="1"
                          Width="4"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          Background="Transparent"/>

            <!-- Plugin List -->
            <Border Grid.Column="2" Background="{DynamicResource BackgroundBrush}">
                <DataGrid x:Name="PluginGrid"
                          ItemsSource="{Binding FilteredPlugins}"
                          SelectedItem="{Binding SelectedPlugin}"
                          AutoGenerateColumns="False"
                          RowStyle="{StaticResource PluginRowStyle}"
                          Background="Transparent"
                          BorderThickness="0"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          CanUserReorderColumns="True"
                          CanUserResizeRows="False"
                          IsReadOnly="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.ColumnHeaderStyle>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#2D2D30"/>
                            <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
                            <Setter Property="Padding" Value="8,6"/>
                            <Setter Property="FontSize" Value="11"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                            <Setter Property="BorderThickness" Value="0,0,1,1"/>
                        </Style>
                    </DataGrid.ColumnHeaderStyle>
                    <DataGrid.Columns>
                        <!-- Favorite -->
                        <DataGridTemplateColumn Header="" Width="36" CanUserResize="False">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ToggleButton IsChecked="{Binding IsFavorite, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  Style="{StaticResource StarButtonStyle}"
                                                  Click="FavoriteToggle_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Name -->
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="*" MinWidth="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Type -->
                        <DataGridTemplateColumn Header="Type" Width="60" CanUserResize="False">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border CornerRadius="3" Padding="6,2" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Background" Value="{StaticResource Vst2BadgeBrush}"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsVst3}" Value="True">
                                                        <Setter Property="Background" Value="{StaticResource Vst3BadgeBrush}"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <TextBlock Text="{Binding FormatDisplay}" FontSize="9" FontWeight="Bold" Foreground="White"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <!-- Vendor -->
                        <DataGridTextColumn Header="Vendor" Binding="{Binding Vendor}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Tags -->
                        <DataGridTextColumn Header="Tags" Binding="{Binding TagsDisplay}" Width="120">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Padding" Value="8,6"/>
                                    <Setter Property="VerticalAlignment" Value="Center"/>
                                    <Setter Property="Foreground" Value="{DynamicResource SecondaryForegroundBrush}"/>
                                    <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>

                        <!-- Enabled -->
                        <DataGridTemplateColumn Header="Enabled" Width="60" CanUserResize="False">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <CheckBox IsChecked="{Binding IsEnabled, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                              HorizontalAlignment="Center"
                                              Click="EnabledToggle_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <GridSplitter Grid.Column="3"
                          Width="4"
                          HorizontalAlignment="Center"
                          VerticalAlignment="Stretch"
                          Background="Transparent"/>

            <!-- Plugin Info Panel -->
            <Border Grid.Column="4"
                    Background="{DynamicResource PanelBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="1,0,0,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="InfoPanel"
                                Visibility="{Binding HasSelectedPlugin, Converter={StaticResource BoolToVisibilityConverter}}"
                                Margin="16">
                        <!-- Plugin Name -->
                        <TextBlock Text="{Binding SelectedPlugin.Name}"
                                   FontSize="16"
                                   FontWeight="SemiBold"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,4"/>

                        <!-- Vendor -->
                        <TextBlock Text="{Binding SelectedPlugin.Vendor}"
                                   FontSize="12"
                                   Foreground="{DynamicResource SecondaryForegroundBrush}"
                                   Margin="0,0,0,16"/>

                        <!-- Info Grid -->
                        <Grid Margin="0,0,0,16">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Grid.Column="0" Text="Format:" FontSize="11" Foreground="{DynamicResource SecondaryForegroundBrush}" Margin="0,2"/>
                            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding SelectedPlugin.FormatDisplay}" FontSize="11" Margin="8,2"/>

                            <TextBlock Grid.Row="1" Grid.Column="0" Text="Version:" FontSize="11" Foreground="{DynamicResource SecondaryForegroundBrush}" Margin="0,2"/>
                            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding SelectedPlugin.Version}" FontSize="11" Margin="8,2"/>

                            <TextBlock Grid.Row="2" Grid.Column="0" Text="Category:" FontSize="11" Foreground="{DynamicResource SecondaryForegroundBrush}" Margin="0,2"/>
                            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SelectedPlugin.Category}" FontSize="11" Margin="8,2"/>

                            <TextBlock Grid.Row="3" Grid.Column="0" Text="Inputs:" FontSize="11" Foreground="{DynamicResource SecondaryForegroundBrush}" Margin="0,2"/>
                            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedPlugin.NumInputs}" FontSize="11" Margin="8,2"/>

                            <TextBlock Grid.Row="4" Grid.Column="0" Text="Outputs:" FontSize="11" Foreground="{DynamicResource SecondaryForegroundBrush}" Margin="0,2"/>
                            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding SelectedPlugin.NumOutputs}" FontSize="11" Margin="8,2"/>

                            <TextBlock Grid.Row="5" Grid.Column="0" Text="Parameters:" FontSize="11" Foreground="{DynamicResource SecondaryForegroundBrush}" Margin="0,2"/>
                            <TextBlock Grid.Row="5" Grid.Column="1" Text="{Binding SelectedPlugin.NumParameters}" FontSize="11" Margin="8,2"/>
                        </Grid>

                        <!-- Path -->
                        <TextBlock Text="Path" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding SelectedPlugin.Path, Mode=OneWay}"
                                 IsReadOnly="True"
                                 Background="#252628"
                                 BorderBrush="{DynamicResource BorderBrush}"
                                 Foreground="{DynamicResource SecondaryForegroundBrush}"
                                 FontSize="10"
                                 TextWrapping="Wrap"
                                 Padding="6"
                                 Margin="0,0,0,16"/>

                        <!-- Tags Editor -->
                        <TextBlock Text="Tags (comma separated)" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,4"/>
                        <TextBox x:Name="TagsTextBox"
                                 Text="{Binding SelectedPlugin.TagsDisplay, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                 Background="#252628"
                                 BorderBrush="{DynamicResource BorderBrush}"
                                 Foreground="{DynamicResource ForegroundBrush}"
                                 FontSize="11"
                                 Padding="6"
                                 Margin="0,0,0,16"
                                 LostFocus="TagsTextBox_LostFocus"/>

                        <!-- Actions -->
                        <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                            <Button Content="Show in Explorer"
                                    Click="ShowInExplorer_Click"
                                    Style="{DynamicResource SecondaryButtonStyle}"
                                    Padding="10,6"
                                    Margin="0,0,8,0"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>

        <!-- Status Bar -->
        <Border Grid.Row="2"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusText}"
                           FontSize="11"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <TextBlock Text="{Binding ScanStatus}"
                               FontSize="11"
                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                               VerticalAlignment="Center"
                               Margin="0,0,16,0"/>
                    <ProgressBar x:Name="ScanProgress"
                                 Width="100"
                                 Height="4"
                                 Visibility="Collapsed"
                                 IsIndeterminate="True"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer Buttons -->
        <Border Grid.Row="3"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="16,12">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Close"
                        Click="CloseButton_Click"
                        Style="{DynamicResource PrimaryButtonStyle}"
                        Padding="20,8"
                        IsDefault="True"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
