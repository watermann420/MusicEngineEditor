<UserControl x:Class="MusicEngineEditor.Views.MarkerListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MusicEngineEditor.Views"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Colors -->
        <Color x:Key="ListBackgroundColor">#1E1F22</Color>
        <Color x:Key="HeaderBackgroundColor">#2B2D30</Color>
        <Color x:Key="RowHoverColor">#3C3F41</Color>
        <Color x:Key="RowSelectedColor">#4B6EAF</Color>
        <Color x:Key="TextPrimaryColor">#BCBEC4</Color>
        <Color x:Key="TextSecondaryColor">#6F737A</Color>
        <Color x:Key="BorderColor">#393B40</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="ListBackgroundBrush" Color="{StaticResource ListBackgroundColor}"/>
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="{StaticResource HeaderBackgroundColor}"/>
        <SolidColorBrush x:Key="RowHoverBrush" Color="{StaticResource RowHoverColor}"/>
        <SolidColorBrush x:Key="RowSelectedBrush" Color="{StaticResource RowSelectedColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="ListBorderBrush" Color="{StaticResource BorderColor}"/>

        <!-- Search Box Style -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#1E1F22"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ListBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="&#x1F50D;"
                                           FontSize="12"
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,4,0"/>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Grid.Column="1"
                                              Margin="0,0,8,0"
                                              VerticalAlignment="Center"/>
                                <TextBlock x:Name="Placeholder"
                                           Grid.Column="1"
                                           Text="Search markers..."
                                           Foreground="{StaticResource TextSecondaryBrush}"
                                           VerticalAlignment="Center"
                                           IsHitTestVisible="False"
                                           Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiTrigger>
                                <MultiTrigger.Conditions>
                                    <Condition Property="Text" Value=""/>
                                    <Condition Property="IsFocused" Value="False"/>
                                </MultiTrigger.Conditions>
                                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible"/>
                            </MultiTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Toolbar Button Style -->
        <Style x:Key="MarkerToolbarButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{StaticResource ListBorderBrush}"
                                BorderThickness="1"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource RowHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource RowSelectedBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Add Button Style -->
        <Style x:Key="AddButtonStyle" TargetType="Button" BasedOn="{StaticResource MarkerToolbarButtonStyle}">
            <Setter Property="Background" Value="#4B6EAF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <!-- Color Indicator Style -->
        <Style x:Key="ColorIndicatorStyle" TargetType="Border">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="CornerRadius" Value="3"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0"
                Background="{StaticResource HeaderBackgroundBrush}"
                BorderBrush="{StaticResource ListBorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="8,6">
            <DockPanel>
                <!-- Left Section: Add Button -->
                <StackPanel DockPanel.Dock="Left" Orientation="Horizontal">
                    <Button Content="+ Add Marker"
                            Style="{StaticResource AddButtonStyle}"
                            Click="AddMarker_Click"
                            ToolTip="Add a new marker (Ctrl+M)"/>

                    <Rectangle Width="1"
                               Fill="{StaticResource ListBorderBrush}"
                               Margin="12,4"/>

                    <Button Content="Delete"
                            Style="{StaticResource MarkerToolbarButtonStyle}"
                            Click="DeleteMarker_Click"
                            IsEnabled="{Binding HasSelection}"
                            ToolTip="Delete selected marker (Del)"/>

                    <Button Content="Edit"
                            Style="{StaticResource MarkerToolbarButtonStyle}"
                            Click="EditMarker_Click"
                            IsEnabled="{Binding HasSelection}"
                            ToolTip="Edit selected marker (Enter)"/>
                </StackPanel>

                <!-- Right Section: Search Box -->
                <TextBox x:Name="SearchBox"
                         DockPanel.Dock="Right"
                         Style="{StaticResource SearchBoxStyle}"
                         Width="200"
                         TextChanged="SearchBox_TextChanged"/>

                <!-- Spacer -->
                <Border/>
            </DockPanel>
        </Border>

        <!-- Marker DataGrid -->
        <DataGrid x:Name="MarkerGrid"
                  Grid.Row="1"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="True"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  GridLinesVisibility="Horizontal"
                  Background="{StaticResource ListBackgroundBrush}"
                  BorderThickness="0"
                  RowHeaderWidth="0"
                  MouseDoubleClick="MarkerGrid_MouseDoubleClick"
                  SelectionChanged="MarkerGrid_SelectionChanged">

            <DataGrid.ContextMenu>
                <ContextMenu>
                    <MenuItem Header="Go To" Click="GoToMarker_Click"/>
                    <MenuItem Header="Edit..." Click="EditMarker_Click"/>
                    <Separator/>
                    <MenuItem Header="Delete" Click="DeleteMarker_Click"/>
                </ContextMenu>
            </DataGrid.ContextMenu>

            <DataGrid.Columns>
                <!-- Color Column -->
                <DataGridTemplateColumn Header="" Width="32" CanUserSort="False" CanUserResize="False">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource ColorIndicatorStyle}"
                                    Background="{Binding Color}"
                                    HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Name Column -->
                <DataGridTextColumn Header="Name"
                                    Binding="{Binding Name}"
                                    Width="*"
                                    MinWidth="100"/>

                <!-- Position Column -->
                <DataGridTextColumn Header="Position"
                                    Binding="{Binding PositionDisplay}"
                                    Width="100"/>

                <!-- Type Column -->
                <DataGridTextColumn Header="Type"
                                    Binding="{Binding TypeDisplay}"
                                    Width="80"/>

                <!-- Notes Column -->
                <DataGridTextColumn Header="Notes"
                                    Binding="{Binding Notes}"
                                    Width="*"
                                    MinWidth="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
