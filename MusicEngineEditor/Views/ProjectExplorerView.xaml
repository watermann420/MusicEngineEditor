<UserControl x:Class="MusicEngineEditor.Views.ProjectExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:MusicEngineEditor.ViewModels">

    <UserControl.Resources>
        <HierarchicalDataTemplate x:Key="FileTreeTemplate" DataType="{x:Type vm:FileTreeNode}"
                                   ItemsSource="{Binding Children}">
            <StackPanel Orientation="Horizontal" Margin="2">
                <!-- Icon based on node type -->
                <TextBlock Width="16" Margin="0,0,4,0" FontFamily="Segoe MDL2 Assets">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding NodeType}" Value="Project">
                                    <Setter Property="Text" Value="&#xE8B7;"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding NodeType}" Value="Folder">
                                    <Setter Property="Text" Value="&#xE8B7;"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding NodeType}" Value="Script">
                                    <Setter Property="Text" Value="&#xE943;"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding NodeType}" Value="Audio">
                                    <Setter Property="Text" Value="&#xE8D6;"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding NodeType}" Value="Reference">
                                    <Setter Property="Text" Value="&#xE71B;"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
                <TextBlock Text="{Binding Name}" VerticalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsEntryPoint}" Value="True">
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </HierarchicalDataTemplate>
    </UserControl.Resources>

    <Grid Background="{DynamicResource PanelBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="{DynamicResource ToolbarBackgroundBrush}" Padding="4">
            <StackPanel Orientation="Horizontal">
                <Button Content="+" ToolTip="Add New Item" Style="{DynamicResource ToolbarButtonStyle}"
                        Command="{Binding AddNewScriptCommand}"/>
                <Button Content="&#xE8F4;" FontFamily="Segoe MDL2 Assets" ToolTip="Refresh"
                        Style="{DynamicResource ToolbarButtonStyle}" Margin="4,0,0,0"/>
            </StackPanel>
        </Border>

        <!-- Tree View -->
        <TreeView Grid.Row="1"
                  ItemsSource="{Binding RootNodes}"
                  ItemTemplate="{StaticResource FileTreeTemplate}"
                  Background="Transparent"
                  BorderThickness="0">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                    <Setter Property="Padding" Value="2"/>
                    <EventSetter Event="MouseDoubleClick" Handler="TreeViewItem_MouseDoubleClick"/>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>
    </Grid>
</UserControl>
