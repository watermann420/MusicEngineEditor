<UserControl x:Class="MusicEngineEditor.Controls.PluginSearchPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MusicEngineEditor.Controls"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="350"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <!-- Colors -->
        <Color x:Key="PanelBackgroundColor">#1E1F22</Color>
        <Color x:Key="HeaderBackgroundColor">#2B2D30</Color>
        <Color x:Key="ItemBackgroundColor">#2B2D30</Color>
        <Color x:Key="ItemHoverColor">#3C3F41</Color>
        <Color x:Key="ItemSelectedColor">#4B6EAF</Color>
        <Color x:Key="TextPrimaryColor">#BCBEC4</Color>
        <Color x:Key="TextSecondaryColor">#6F737A</Color>
        <Color x:Key="BorderColor">#393B40</Color>
        <Color x:Key="BypassedColor">#FF5555</Color>

        <!-- Brushes -->
        <SolidColorBrush x:Key="PanelBackgroundBrush" Color="{StaticResource PanelBackgroundColor}"/>
        <SolidColorBrush x:Key="HeaderBackgroundBrush" Color="{StaticResource HeaderBackgroundColor}"/>
        <SolidColorBrush x:Key="ItemBackgroundBrush" Color="{StaticResource ItemBackgroundColor}"/>
        <SolidColorBrush x:Key="ItemHoverBrush" Color="{StaticResource ItemHoverColor}"/>
        <SolidColorBrush x:Key="ItemSelectedBrush" Color="{StaticResource ItemSelectedColor}"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="{StaticResource TextPrimaryColor}"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="{StaticResource TextSecondaryColor}"/>
        <SolidColorBrush x:Key="PluginBorderBrush" Color="{StaticResource BorderColor}"/>
        <SolidColorBrush x:Key="BypassedBrush" Color="{StaticResource BypassedColor}"/>

        <!-- Filter Button Style -->
        <Style x:Key="FilterButtonStyle" TargetType="ToggleButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextSecondaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PluginBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="2,0"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ItemHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource ItemSelectedBrush}"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource ItemSelectedBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Plugin Item Template -->
        <DataTemplate x:Key="PluginItemTemplate">
            <Border x:Name="ItemBorder"
                    Background="{StaticResource ItemBackgroundBrush}"
                    BorderBrush="{StaticResource PluginBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="4"
                    Margin="0,2"
                    Padding="8,6"
                    Cursor="Hand">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Plugin Color Indicator -->
                    <Border Grid.Column="0"
                            Grid.RowSpan="2"
                            Width="4"
                            CornerRadius="2"
                            Margin="0,0,8,0"
                            Background="{Binding CategoryColor}"/>

                    <!-- Plugin Name -->
                    <TextBlock Grid.Column="1"
                               Grid.Row="0"
                               Text="{Binding Name}"
                               Foreground="{StaticResource TextPrimaryBrush}"
                               FontSize="12"
                               FontWeight="Medium"
                               TextTrimming="CharacterEllipsis"/>

                    <!-- Plugin Category and Track -->
                    <StackPanel Grid.Column="1"
                                Grid.Row="1"
                                Orientation="Horizontal">
                        <TextBlock Text="{Binding Category}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="10"/>
                        <TextBlock Text=" | "
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="10"/>
                        <TextBlock Text="{Binding TrackName}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="10"/>
                    </StackPanel>

                    <!-- Status Indicators -->
                    <StackPanel Grid.Column="2"
                                Grid.RowSpan="2"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <!-- Slot Index -->
                        <TextBlock Text="{Binding SlotIndex, StringFormat='[{0}]'}"
                                   Foreground="{StaticResource TextSecondaryBrush}"
                                   FontSize="10"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0"/>

                        <!-- Bypass Indicator -->
                        <Border x:Name="BypassIndicator"
                                Width="8" Height="8"
                                CornerRadius="4"
                                Background="{StaticResource BypassedBrush}"
                                Visibility="Collapsed"
                                ToolTip="Bypassed"/>
                    </StackPanel>
                </Grid>
            </Border>
            <DataTemplate.Triggers>
                <DataTrigger Binding="{Binding IsBypassed}" Value="True">
                    <Setter TargetName="BypassIndicator" Property="Visibility" Value="Visible"/>
                    <Setter TargetName="ItemBorder" Property="Opacity" Value="0.6"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding IsSelected}" Value="True">
                    <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource ItemSelectedBrush}"/>
                    <Setter TargetName="ItemBorder" Property="BorderBrush" Value="{StaticResource ItemSelectedBrush}"/>
                </DataTrigger>
                <Trigger SourceName="ItemBorder" Property="IsMouseOver" Value="True">
                    <Setter TargetName="ItemBorder" Property="Background" Value="{StaticResource ItemHoverBrush}"/>
                </Trigger>
            </DataTemplate.Triggers>
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search Box -->
        <Border Grid.Row="0"
                Background="{StaticResource PanelBackgroundBrush}"
                BorderBrush="{StaticResource PluginBorderBrush}"
                BorderThickness="1"
                CornerRadius="4"
                Margin="8,8,8,4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="&#x1F50D;"
                           FontSize="12"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           VerticalAlignment="Center"
                           Margin="10,0,6,0"/>

                <TextBox x:Name="SearchBox"
                         Grid.Column="1"
                         Background="Transparent"
                         Foreground="{StaticResource TextPrimaryBrush}"
                         BorderThickness="0"
                         Padding="0,8"
                         FontSize="12"
                         CaretBrush="{StaticResource TextPrimaryBrush}"
                         TextChanged="SearchBox_TextChanged"/>

                <TextBlock x:Name="SearchPlaceholder"
                           Grid.Column="1"
                           Text="Search plugins..."
                           Foreground="{StaticResource TextSecondaryBrush}"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"/>
            </Grid>
        </Border>

        <!-- Filter Buttons -->
        <Border Grid.Row="1"
                Padding="8,4">
            <WrapPanel>
                <ToggleButton x:Name="FilterAll"
                              Content="All"
                              Style="{StaticResource FilterButtonStyle}"
                              IsChecked="True"
                              Checked="FilterAll_Checked"/>
                <ToggleButton x:Name="FilterEQ"
                              Content="EQ"
                              Style="{StaticResource FilterButtonStyle}"
                              Checked="Filter_Checked"
                              Unchecked="Filter_Unchecked"/>
                <ToggleButton x:Name="FilterCompressor"
                              Content="Comp"
                              Style="{StaticResource FilterButtonStyle}"
                              Checked="Filter_Checked"
                              Unchecked="Filter_Unchecked"/>
                <ToggleButton x:Name="FilterReverb"
                              Content="Reverb"
                              Style="{StaticResource FilterButtonStyle}"
                              Checked="Filter_Checked"
                              Unchecked="Filter_Unchecked"/>
                <ToggleButton x:Name="FilterDelay"
                              Content="Delay"
                              Style="{StaticResource FilterButtonStyle}"
                              Checked="Filter_Checked"
                              Unchecked="Filter_Unchecked"/>
                <ToggleButton x:Name="FilterModulation"
                              Content="Mod"
                              Style="{StaticResource FilterButtonStyle}"
                              Checked="Filter_Checked"
                              Unchecked="Filter_Unchecked"/>
                <ToggleButton x:Name="FilterDistortion"
                              Content="Dist"
                              Style="{StaticResource FilterButtonStyle}"
                              Checked="Filter_Checked"
                              Unchecked="Filter_Unchecked"/>
                <ToggleButton x:Name="FilterVST"
                              Content="VST"
                              Style="{StaticResource FilterButtonStyle}"
                              Checked="Filter_Checked"
                              Unchecked="Filter_Unchecked"/>
            </WrapPanel>
        </Border>

        <!-- Plugin List -->
        <ListBox x:Name="PluginList"
                 Grid.Row="2"
                 Background="Transparent"
                 BorderThickness="0"
                 Padding="8,0"
                 ItemTemplate="{StaticResource PluginItemTemplate}"
                 SelectionChanged="PluginList_SelectionChanged"
                 MouseDoubleClick="PluginList_MouseDoubleClick"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <ContentPresenter/>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>

        <!-- Plugin Preview Panel -->
        <Border x:Name="PreviewPanel"
                Grid.Row="3"
                Background="{StaticResource HeaderBackgroundBrush}"
                BorderBrush="{StaticResource PluginBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,8"
                Visibility="Collapsed">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Plugin Name -->
                <TextBlock x:Name="PreviewName"
                           Grid.Column="0"
                           Grid.Row="0"
                           Text="Plugin Name"
                           FontSize="12"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"/>

                <!-- Go To Button -->
                <Button Grid.Column="1"
                        Grid.RowSpan="2"
                        Content="Go To"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="White"
                        BorderThickness="0"
                        Padding="12,6"
                        Cursor="Hand"
                        Click="GoToPlugin_Click">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#5A7FC4"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Parameters Preview -->
                <TextBlock x:Name="PreviewParams"
                           Grid.Column="0"
                           Grid.Row="1"
                           Text="Parameters: ..."
                           FontSize="10"
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,4,0,0"
                           TextTrimming="CharacterEllipsis"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
