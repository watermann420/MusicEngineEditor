<UserControl x:Class="MusicEngineEditor.Controls.AudioFileBrowser"
             x:Name="AudioFileBrowserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:MusicEngineEditor.Controls"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="350"
             Background="{DynamicResource BackgroundBrush}">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>

        <!-- Folder Tree Item Style -->
        <Style x:Key="FolderTreeItemStyle" TargetType="TreeViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Padding" Value="4,2"/>
            <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TreeViewItem">
                        <StackPanel>
                            <Border x:Name="Bd"
                                    Background="{TemplateBinding Background}"
                                    Padding="{TemplateBinding Padding}"
                                    CornerRadius="4"
                                    Margin="0,1">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <ToggleButton x:Name="Expander"
                                                  IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}"
                                                  ClickMode="Press"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Width="16" Height="16"
                                                  Margin="0,0,4,0">
                                        <ToggleButton.Template>
                                            <ControlTemplate TargetType="ToggleButton">
                                                <Border Background="Transparent">
                                                    <TextBlock x:Name="arrow"
                                                               Text="&#x25B6;"
                                                               FontSize="8"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"
                                                               Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsChecked" Value="True">
                                                        <Setter TargetName="arrow" Property="Text" Value="&#x25BC;"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </ToggleButton.Template>
                                    </ToggleButton>

                                    <TextBlock Grid.Column="1"
                                               Text="&#x1F4C1;"
                                               FontSize="14"
                                               VerticalAlignment="Center"
                                               Margin="0,0,6,0"/>

                                    <ContentPresenter Grid.Column="2"
                                                      ContentSource="Header"
                                                      HorizontalAlignment="Left"
                                                      VerticalAlignment="Center"/>

                                    <Button Grid.Column="3"
                                            Content="&#x2605;"
                                            FontSize="10"
                                            Background="Transparent"
                                            BorderThickness="0"
                                            Foreground="{DynamicResource SecondaryForegroundBrush}"
                                            Padding="4,0"
                                            Cursor="Hand"
                                            Visibility="{Binding IsFavorite, Converter={StaticResource BoolToVisibilityConverter}}"
                                            ToolTip="Favorite folder"/>
                                </Grid>
                            </Border>
                            <ItemsPresenter x:Name="ItemsHost" Margin="16,0,0,0"/>
                        </StackPanel>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="Expander" Property="Visibility" Value="Hidden"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3C3F41"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- File List Item Style -->
        <Style x:Key="FileListItemStyle" TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,1"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                CornerRadius="4">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3C3F41"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Search Box Style -->
        <Style x:Key="SearchBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource SubtleBorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="{DynamicResource ForegroundBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="&#x1F50D;"
                                           FontSize="12"
                                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,4,0"/>
                                <ScrollViewer x:Name="PART_ContentHost"
                                              Grid.Column="1"
                                              Padding="{TemplateBinding Padding}"
                                              VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,0,0,1"
                Padding="12,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#x1F4C2;"
                               FontSize="16"
                               VerticalAlignment="Center"
                               Margin="0,0,8,0"/>
                    <TextBlock Text="Audio Browser"
                               FontSize="14"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource BrightForegroundBrush}"
                               VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="&#x2606;"
                            ToolTip="Add to favorites"
                            Command="{Binding AddFavoriteCommand}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="6"
                            Cursor="Hand"
                            FontSize="14"
                            Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                    <Button Content="&#x2302;"
                            ToolTip="Go to user folder"
                            Command="{Binding GoHomeCommand}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="6"
                            Cursor="Hand"
                            FontSize="14"
                            Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Search Box -->
        <Border Grid.Row="1" Padding="12,8">
            <TextBox x:Name="SearchBox"
                     Style="{StaticResource SearchBoxStyle}"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     ToolTip="Search files (Ctrl+F)"/>
        </Border>

        <!-- Format Filter -->
        <Border Grid.Row="2" Padding="12,0,12,8">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Filter:"
                           VerticalAlignment="Center"
                           Margin="0,0,8,0"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           FontSize="11"/>
                <ComboBox ItemsSource="{Binding FormatFilters}"
                          SelectedItem="{Binding SelectedFormatFilter}"
                          Width="100"
                          Background="{DynamicResource EditorBackgroundBrush}"
                          Foreground="{DynamicResource ForegroundBrush}"
                          BorderBrush="{DynamicResource BorderBrush}"
                          FontSize="11"/>
            </StackPanel>
        </Border>

        <!-- Favorites Bar -->
        <Border Grid.Row="3"
                Background="{DynamicResource PanelBackgroundBrush}"
                Padding="8,4"
                Visibility="{Binding HasFavorites, Converter={StaticResource BoolToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding FavoriteFolders}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Name}"
                                Command="{Binding DataContext.NavigateToFavoriteCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                Background="Transparent"
                                BorderThickness="0"
                                Padding="8,4"
                                Margin="2"
                                Cursor="Hand"
                                FontSize="11">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Foreground" Value="{DynamicResource ForegroundBrush}"/>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="{DynamicResource AccentBrush}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- File List -->
        <Grid Grid.Row="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Current Path -->
            <Border Grid.Row="0"
                    Background="{DynamicResource EditorBackgroundBrush}"
                    BorderBrush="{DynamicResource BorderBrush}"
                    BorderThickness="0,0,0,1"
                    Padding="12,6">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Content="&#x2191;"
                            ToolTip="Go up"
                            Command="{Binding GoUpCommand}"
                            Background="Transparent"
                            BorderThickness="0"
                            Padding="4"
                            Cursor="Hand"
                            FontSize="12"
                            Foreground="{DynamicResource ForegroundBrush}"
                            Margin="0,0,8,0"/>

                    <TextBlock Grid.Column="1"
                               Text="{Binding CurrentPath}"
                               FontSize="11"
                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                               VerticalAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               ToolTip="{Binding CurrentPath}"/>
                </Grid>
            </Border>

            <!-- File/Folder List -->
            <ListView Grid.Row="1"
                      x:Name="FileListView"
                      ItemsSource="{Binding FilteredItems}"
                      SelectedItem="{Binding SelectedItem}"
                      Background="Transparent"
                      BorderThickness="0"
                      Padding="8,4"
                      ItemContainerStyle="{StaticResource FileListItemStyle}"
                      MouseDoubleClick="FileListView_MouseDoubleClick"
                      KeyDown="FileListView_KeyDown"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      AllowDrop="False">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="60"/>
                            </Grid.ColumnDefinitions>

                            <!-- Icon -->
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Icon}"
                                       FontSize="14"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"/>

                            <!-- Name and Info -->
                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                <TextBlock Text="{Binding Name}"
                                           FontSize="12"
                                           FontWeight="Medium"
                                           TextTrimming="CharacterEllipsis"/>
                                <StackPanel Orientation="Horizontal"
                                            Visibility="{Binding IsFile, Converter={StaticResource BoolToVisibilityConverter}}">
                                    <TextBlock Text="{Binding Format}"
                                               FontSize="10"
                                               Foreground="{DynamicResource SecondaryForegroundBrush}"/>
                                    <TextBlock Text=" | "
                                               FontSize="10"
                                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                                               Visibility="{Binding HasMetadata, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                    <TextBlock Text="{Binding Metadata}"
                                               FontSize="10"
                                               Foreground="{DynamicResource SecondaryForegroundBrush}"
                                               Visibility="{Binding HasMetadata, Converter={StaticResource BoolToVisibilityConverter}}"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- BPM Badge -->
                            <Border Grid.Column="2"
                                    Background="{DynamicResource SubtleBorderBrush}"
                                    CornerRadius="3"
                                    Padding="6,2"
                                    Margin="4,0"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding HasBpm, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="{Binding BpmDisplay}"
                                           FontSize="10"
                                           Foreground="{DynamicResource ForegroundBrush}"/>
                            </Border>

                            <!-- Key Badge -->
                            <Border Grid.Column="3"
                                    Background="{DynamicResource AccentBrush}"
                                    CornerRadius="3"
                                    Padding="6,2"
                                    Margin="4,0"
                                    VerticalAlignment="Center"
                                    Visibility="{Binding HasKey, Converter={StaticResource BoolToVisibilityConverter}}">
                                <TextBlock Text="{Binding KeyDisplay}"
                                           FontSize="10"
                                           Foreground="White"/>
                            </Border>

                            <!-- Duration -->
                            <TextBlock Grid.Column="4"
                                       Text="{Binding DurationDisplay}"
                                       FontSize="10"
                                       Foreground="{DynamicResource SecondaryForegroundBrush}"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Right"
                                       Visibility="{Binding IsFile, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>

        <!-- Waveform Preview -->
        <Border Grid.Row="5"
                Background="{DynamicResource EditorBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Height="60"
                Visibility="{Binding ShowWaveformPreview, Converter={StaticResource BoolToVisibilityConverter}}">
            <Grid>
                <!-- Waveform Canvas -->
                <Canvas x:Name="WaveformCanvas" ClipToBounds="True"/>

                <!-- Loading indicator -->
                <TextBlock Text="Loading waveform..."
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           FontSize="10"
                           Visibility="{Binding IsLoadingWaveform, Converter={StaticResource BoolToVisibilityConverter}}"/>

                <!-- File info overlay -->
                <StackPanel Orientation="Horizontal"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Bottom"
                            Margin="8,4">
                    <TextBlock Text="{Binding PreviewFileName}"
                               FontSize="10"
                               Foreground="{DynamicResource ForegroundBrush}"
                               FontWeight="Medium"/>
                </StackPanel>

                <!-- Play indicator -->
                <Border HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Margin="8,4"
                        Background="{DynamicResource RunButtonBrush}"
                        CornerRadius="3"
                        Padding="6,2"
                        Visibility="{Binding IsPreviewPlaying, Converter={StaticResource BoolToVisibilityConverter}}">
                    <TextBlock Text="Playing"
                               FontSize="9"
                               Foreground="White"/>
                </Border>
            </Grid>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="6"
                Background="{DynamicResource PanelBackgroundBrush}"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="12,6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding StatusMessage}"
                           FontSize="11"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1"
                           Text="Space: Preview"
                           FontSize="10"
                           Foreground="{DynamicResource SecondaryForegroundBrush}"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0"/>

                <Button Grid.Column="2"
                        Content="Import to Timeline"
                        Command="{Binding ImportCommand}"
                        IsEnabled="{Binding CanImport}"
                        Padding="12,4"
                        FontSize="11"
                        Background="{DynamicResource AccentBrush}"
                        Foreground="White"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
